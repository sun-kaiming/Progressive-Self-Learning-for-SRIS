1.数据在data文件夹下
2.首先配置好conda虚拟环境
3.测试：
    1. 然后可以在终端或者直接运行train.py文件进行测试。
    2. 推荐在终端使用命令进行测试，命令在“2.测试命令”文件里面
        1.结果可以直接在终端看到，结果也被保存在了“./test_result”目录下，保存的文件是你csv结果，最好用excel查看
    3. 目前生成新数据操作在台式机上有些问题（可能是没有显卡的原因）
